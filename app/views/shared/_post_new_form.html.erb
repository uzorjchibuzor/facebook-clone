<h1> Create New Post </h1>

<%= form_for (Post.new) do |f| %>
<%= f.text_area :content, cols: "30", rows: "10"%>
<%= f.label :picture, "Add image" %><br />
<%= f.file_field :picture %>
<%= f.submit "Create Post" %>
<% end %>